// node_modules/@spectrum-web-components/shared/src/focusable-selectors.dev.js
var focusables = [
  "input:not([inert])",
  "select:not([inert])",
  "textarea:not([inert])",
  "a[href]:not([inert])",
  "button:not([inert])",
  "label:not([inert])",
  "[tabindex]:not([inert])",
  "audio[controls]:not([inert])",
  "video[controls]:not([inert])",
  '[contenteditable]:not([contenteditable="false"]):not([inert])',
  "details>summary:first-of-type:not([inert])",
  "details:not([inert])",
  '[focusable]:not([focusable="false"])'
  // custom dev use-case
];
var userFocuable = ':not([tabindex="-1"])';
var userFocusableSelector = focusables.join(`${userFocuable}, `) + userFocuable;
var focusableSelector = focusables.join(", ");

// node_modules/@spectrum-web-components/shared/src/first-focusable-in.dev.js
var firstFocusableIn = (root) => {
  const firstFocusable = root.querySelector(
    userFocusableSelector
  );
  return firstFocusable;
};
var firstFocusableSlottedIn = (root) => {
  const firstFocusable = root.assignedElements().find(
    (element) => element.matches(userFocusableSelector)
  );
  return firstFocusable;
};

export {
  userFocusableSelector,
  focusableSelector,
  firstFocusableIn,
  firstFocusableSlottedIn
};
//# sourceMappingURL=chunk-L7KH37RZ.js.map
